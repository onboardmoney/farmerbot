{
  "name": "@farmerbot/monorepo",
  "version": "1.0.0",
  "keywords": [
    "ethereum",
    "react",
    "workspaces",
    "yarn"
  ],
  "private": true,
  "scripts": {
    "subgraph:auth": "yarn workspace @farmerbot/subgraph auth",
    "subgraph:codegen": "yarn workspace @farmerbot/subgraph codegen",
    "subgraph:build": "yarn workspace @farmerbot/subgraph build",
    "subgraph:deploy": "yarn workspace @farmerbot/subgraph deploy",
    "react-app:build": "yarn workspace @farmerbot/react-app build",
    "react-app:eject": "yarn workspace @farmerbot/react-app eject",
    "react-app:start": "yarn workspace @farmerbot/react-app start",
    "react-app:test": "yarn workspace @farmerbot/react-app test",
    "bot:start": "yarn workspace @farmerbot/bot start"
    
  },
  "workspaces": {
    "nohoist": [
      "**/@graphprotocol/graph-ts",
      "**/@graphprotocol/graph-ts/**"
    ],
    "packages": [
      "packages/*"
    ]
  }
}
